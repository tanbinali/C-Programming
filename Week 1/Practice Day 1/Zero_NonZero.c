#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int N;
    scanf("%d", &N);
    if (N==0) printf("Zero");
    else printf("Non Zero");
    return 0;
}